# docs

`screenshots/` holds dashboard captures used in issue write-ups.

There is nothing to index here. This file used to be generated by
`scripts/validate-rules.py`, which v1.0.0 deleted along with the filesystem
corpus it walked. Standards now live in SQLite and are listed by
`playbook_find_standards` or the dashboard's Standards page, not by a
checked-in index.
