{
  "mcpServers": {
    "dev-playbook": {
      "type": "sse",
      "url": "${user_config.server_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.token}",
        "X-MCP-Editor": "claude-code"
      }
    }
  }
}
